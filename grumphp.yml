parameters:
    git_dir: .
    bin_dir: ./vendor/bin
    tasks:
        phpcs:
            standard: "PSR2"
        git_blacklist:
            keywords:
                - "die("
                - "die;"
                - "exit("
                - "exit;"
                - "print_r("
                - "var_dump("
